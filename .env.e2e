# Shared image-tag pin for the e2e stack at docker/servatrice/. Bumping the
# Servatrice release is a one-line edit here. Consumed via the
# `--env-file ../../.env.e2e` flag in each package's test:e2e:up/down script.
SERVATRICE_IMAGE=ghcr.io/cockatrice/servatrice:2026-05-08-Release-3.0.0
