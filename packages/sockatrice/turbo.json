{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/vendor/cockatrice/libcockatrice_protocol/**/*.proto"],
      "outputs": ["dist/**", "src/generated/**"]
    }
  }
}
