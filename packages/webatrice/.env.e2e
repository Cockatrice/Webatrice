# Per-package values for the shared e2e stack at docker/servatrice/.
# Passed to docker-compose via `--env-file .env.e2e`.
# Named `.env.e2e` rather than `.env` so Vite/vitest don't auto-load it
# during dev/test/build.
COMPOSE_PROJECT_NAME=webatrice-e2e
E2E_SERVATRICE_PORT=4748
